# Ray Peat Transcription Project Requirements

# Core dependencies for audio processing and transcription
openai-whisper>=20231117
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.21.0

# Web scraping and RSS parsing
requests>=2.28.0
beautifulsoup4>=4.11.0

# Data processing and file handling
pathlib>=1.0.1
typing>=3.7.4

# Command line interface
argparse>=1.1

# Optional: For advanced speaker diarization (if using pyannote)
# pyannote-audio>=3.0.0

# Optional: For GPU acceleration (if available)
# CUDA should be installed separately for GPU support
